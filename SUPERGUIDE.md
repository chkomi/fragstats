# SUPERGUIDE: AI 협업을 위한 상세 가이드라인

## 개요

본 문서는 인간 전문가와 AI 협업자가 복잡한 프로젝트(예: 연구 보고서, 기술 문서, 정책 제안서 작성 등)를 효율적이고 일관되게 수행하기 위한 표준 작업 절차와 원칙을 규정합니다. AI는 본 가이드라인을 숙지하고 모든 작업을 명시된 절차에 따라 수행해야 합니다.

---

## 1. 프로젝트 초기 설정 및 분석 (Phase 1: Setup & Analysis)

모든 작업은 사용자의 요구사항을 명확히 이해하고 프로젝트의 현재 상태를 파악하는 것에서 시작합니다.

1.  **목표 이해**: 사용자의 최종 목표가 무엇인지 명확히 파악합니다. 모호한 부분이 있다면 구체적인 질문을 통해 요구사항을 명확히 합니다.
2.  **프로젝트 구조 분석**:
    *   `ls -R`과 같은 명령을 통해 현재 작업 중인 프로젝트의 전체 디렉토리 구조와 파일 목록을 파악합니다.
    *   주요 파일(예: 소스코드, 원고, 데이터)과 보조 파일(예: 임시 파일, 로그)을 구분합니다.
    *   기존 파일들의 명명 규칙(naming convention), 사용 언어, 문서 형식(예: Markdown, HWPX 변환용 TXT) 등을 분석하여 프로젝트의 스타일을 파악합니다.
3.  **작업 계획 수립 및 제안**:
    *   파악된 내용을 바탕으로, 사용자의 목표를 달성하기 위한 구체적이고 단계적인 작업 계획을 수립합니다.
    *   "먼저 A 문서를 읽고 B 분석 결과를 요약한 뒤, 이를 바탕으로 C 문서를 수정하겠습니다."와 같이 명확한 계획을 사용자에게 먼저 제안하고 동의를 구합니다. 중대한 작업을 AI 단독으로 결정하고 진행하지 않습니다.

## 2. 콘텐츠 생성 및 수정 원칙 (Phase 2: Content Generation & Revision)

콘텐츠 작업 시 가장 중요한 원칙은 '일관성'과 '근거 기반'입니다.

1.  **일관성 유지**:
    *   **스타일 모방**: 기존 문서의 어조, 문체, 전문 용어, 서식(예: 머리말, 표 형식)을 그대로 모방하여, 새로운 내용이 이질감 없이 통합되도록 합니다.
    *   **구조적 일관성**: 새로운 챕터나 섹션을 추가할 때는 기존 문서의 구조(예: 서론-본론-결론, 가-나-다)를 따릅니다.
2.  **근거 기반 작성**:
    *   **주관 배제**: 특히 분석, 연구, 정책 관련 문서에서는 AI가 임의의 사실이나 수치(예: 가중치, 통계)를 생성해서는 안 됩니다.
    *   **자료 활용**: 사용자가 제공한 데이터, 분석 결과 파일, 참고 문헌의 내용을 명시적인 근거로 활용하여 모든 내용을 작성해야 합니다. "A 분석 결과에 따르면..."과 같이 출처를 밝히며 서술하는 것을 원칙으로 합니다.
3.  **점진적 수정 및 버전 관리**:
    *   사용자의 피드백(예: "문단 형식으로 수정해줘", "특수문자 빼줘")을 받으면, 이를 반영한 새로운 버전의 파일을 생성하는 것을 원칙으로 합니다. 그리고 기본적으로 특수문자의 과도한 사용은 지양합니다.
    *   예: `document_v1.md` -> `document_v2.md` 또는 `document_final.md`
    *   이를 통해 수정 이력을 추적하고, 사용자가 이전 버전과 비교할 수 있도록 합니다.

## 3. 파일 관리 및 버전 관리 (Phase 3: File & Version Management)

체계적인 파일 및 버전 관리는 프로젝트의 무결성을 유지하는 데 필수적입니다.

1.  **파일 명명 및 경로**:
    *   새 파일을 생성할 때는 기존 파일들의 명명 규칙을 따릅니다.
    *   파일은 가장 논리적인 디렉토리에 위치시킵니다. 예를 들어, 원고의 새로운 챕터는 `reports/01_manuscripts/` 와 같은 폴더에 저장합니다. 사용자의 지시가 명확하지 않을 경우, 가장 적합한 위치를 제안합니다.
2.  **Git 버전 관리**:
    *   **상태 확인 (`git status`)**: 커밋 전 항상 `git status` 명령으로 변경된 파일 목록을 확인합니다.
    *   **선별적 추가 (`git add <파일명>`)**: 사용자의 다른 작업에 영향을 주지 않도록, AI가 직접 생성하거나 수정한 파일만 선별하여 스테이징(staging)합니다. `git add .` 명령어의 사용은 지양합니다.
    *   **커밋 메시지 스타일 확인 (`git log -n 3`)**: 기존 커밋 메시지의 형식(예: `docs:`, `feat:`)과 언어를 확인하고, 동일한 스타일로 새로운 커밋 메시지를 작성합니다.
    *   **상세한 커밋 메시지 작성**: 커밋 메시지는 제목뿐만 아니라, 본문에 주요 변경 사항을 목록 형태로 상세히 기술하여 다른 협업자가 변경 내역을 쉽게 파악할 수 있도록 합니다.
    *   **사용자 확인**: `git commit` 및 `git push` 명령어는 실행 전, 항상 사용자에게 해당 명령어의 역할과 커밋 메시지 내용을 먼저 설명하고 실행 동의를 얻습니다.

## 4. 채팅 기록 관리 (Phase 4: History Management)

모든 작업 세션의 맥락을 보존하고 추적하기 위해 채팅 기록을 관리합니다.

1.  **작업 단위**: 하나의 주요 사용자 요청이 완료되는 시점을 작업 단위로 간주합니다.
2.  **기록 절차**:
    *   **폴더 확인 및 생성**: 프로젝트 최상위 디렉토리에 `history` 폴더가 있는지 확인하고, 없으면 생성(`mkdir history`)합니다.
    *   **파일 생성**: `history` 폴더 내에 `YYYYMMDD_핵심요청_작업요약.txt` 형식으로 텍스트 파일을 생성합니다.
        *   `YYYYMMDD`: 작업 날짜 (예: `20251121`)
        *   `핵심요청`: 해당 세션의 가장 중요한 요청 키워드 (예: `정책제안서_수정`, `결론작성`)
        *   `작업요약`: AI가 수행한 핵심 작업 요약 (예: `6장_7장_작성_및_커밋`)
    *   **내용 기록**: 생성된 파일 내에 아래와 같은 형식으로 작업 로그를 간략히 기록합니다.
        *   사용자 요청: (예: 6장과 7장을 최종 연구 결과에 맞게 수정하고 커밋해달라)
        *   AI 수행 작업: (예: 6-1-2, 6-1-3, 6-1-4, 7-1 파일 생성 및 수정. 최종본 커밋 및 푸시 완료.)
        *   주요 산출물: (예: `임시폴더/6-1-4.md`, `7-1.md`)

## 5. 최종 결과물 정리 및 보고 (Phase 5: Finalization & Reporting)

모든 작업이 완료되면, 최종적으로 정리된 내용을 보고합니다.

1.  **최종 요약**: 전체 요청에 대해 어떤 작업이 수행되었고, 어떤 파일들이 생성 또는 수정되었는지 최종적으로 요약하여 보고합니다.
2.  **대기**: 사용자의 추가 지시가 있을 때까지 대기합니다.
